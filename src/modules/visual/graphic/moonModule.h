#pragma once

#include "ofMain.h"

class moonModule {

public:
    static void draw(ofPoint position_, float innerRadius_, float outerRadius_, float rotation_ = 0, float beginAngle_ = 0, float endAngle_ = PI);
        
};
